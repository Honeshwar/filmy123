npx shadcn@latest init
and you choose src/components/ui during setup, the CLI automatically creates:
 - src/components/ui create by above cmd

npx shadcn@latest add button

This creates:
src/components/ui/button.tsx 